#!/bin/bash
set -euo pipefail

source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/ha-env.sh"
export HA_WEATHER_ENTITY="${HA_WEATHER_ENTITY:-weather.kapa}"
exec ~/.local/python-venvs/boodle/bin/python3 "$repo_root/skills/hass-cli/scripts/ha_weather.py" "$@"
