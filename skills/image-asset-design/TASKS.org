#+title: Image Asset Design Implementation Tasks
#+date: 2026-09-14
#+filetags: :ai:images:pi:skills:
#+todo: TODO STARTED BLOCKED | DONE DEFERRED

* DONE Capture the initial capability boundary
CLOSED: [2026-09-14 Mon]

- Recorded the decision to build a narrow locally owned Pi extension and an
  AIPIHKAL companion skill.
- Chose OpenRouter's dedicated Image API and Pi provider-registry authentication
  as the initial direction.
- Deliberately omitted =SKILL.md= while the capability was unimplemented; added
  it only after the extension and deterministic tool tests existed.
- Recorded third-party packages as implementation references rather than
  dependencies.

* TODO Finalize the split-repository and persistence boundaries

- Repository ownership is decided: extension code and tests live in
  =pi-gregoryg=; reusable skill doctrine lives in =AIPIHKAL=.
- Choose the long-term project-memory home without duplicating the full plan in
  both repositories.
- Move =PLANNING.org= and =TASKS.org= out of the skill directory once that home
  exists.
- Decide the default draft-artifact directory and cleanup policy.
- Decide after live acceptance whether fixed model/output policy needs trusted
  global settings; do not add a noninteractive permission casually.
- Keep credentials in Pi's provider registry; do not add token files or duplicate
  environment variables.

* STARTED Freeze the MVP contract against current APIs

- Re-read the installed Pi extension, package, session, and TUI documentation.
- Inspect the current OpenRouter Image API, image-model catalog, endpoint
  capabilities, pricing records, request schema, and response schema.
- Confirm a single initial raster model and supported aspect-ratio/resolution
  values.
- Measure representative response sizes and generation durations, then revise
  hard limits if live evidence warrants it.
- Record which provider-reported usage and cost fields are dependable.
- Decide the tool's behavior when capability discovery is unavailable or stale.

* DONE Scaffold the Pi extension before activating its tool
CLOSED: [2026-09-14 Mon]

- Created an inert extension entry point under
  =pi-gregoryg/DOT-pi/extensions/image-generation/= before policy code existed.
- Added pure client modules and repository-style Node tests without introducing
  a package dependency merely for scaffolding.
- Registered the tool only after client, artifact, and orchestration tests
  passed, then smoke-loaded the extension explicitly without a global reload.
- Initialized =image-asset-design= only when the executable capability was ready
  to accompany it.

* DONE Implement the bounded OpenRouter image client
CLOSED: [2026-09-14 Mon]

- Resolve the =openrouter= API key through Pi's model registry; keep the
  dedicated public Image API origin fixed in code.
- Restrict requests to that HTTPS origin and refuse redirects.
- Use the single live-confirmed static capability profile rather than adding a
  runtime model-catalog dependency to the first tool.
- Validate prompt length, aspect ratio, resolution, and one-image semantics
  before proposing a request.
- Apply cancellation and one explicit total deadline to network and decode work.
- Read success and error bodies with strict byte ceilings.
- Reject malformed JSON, invalid base64, missing images, extra images, oversized
  images, MIME/signature disagreement, and impossible dimensions.
- Normalize usage, cost, model, media type, and dimensions into a compact internal
  result.
- Do not automatically retry failed or uncertain billable requests.

* DONE Implement safe artifact output
CLOSED: [2026-09-14 Mon]

- Sanitize optional basenames and validate any caller-selected output directory.
- Keep default drafts outside the repository and group them by Pi session.
- Require explicit project-local paths for assets written into a worktree.
- Refuse symlink escapes and paths outside the permitted root.
- Reserve output files atomically and never overwrite an existing artifact.
- Write the image and metadata sidecar transactionally enough to report partial
  persistence truthfully.
- Include timestamps, model/options, prompt, dimensions, media type, SHA-256,
  duration, and provider-reported usage/cost in the sidecar.
- Exclude credentials, request headers, raw API payloads, and image base64 from
  logs and metadata.

* DONE Register the billable =image_generate= tool
CLOSED: [2026-09-14 Mon]

- Expose only =prompt=, =aspect_ratio=, =resolution=, =filename=, and
  =output_directory= for the MVP.
- Require interactive confirmation showing model, requested shape, and a bounded
  prompt summary before the provider call.
- Refuse noninteractive execution for the MVP; defer any permission override to
  a separate threat-model decision.
- Stream terse progress through =onUpdate= without exposing prompt or provider
  payload details unnecessarily.
- Return concise text metadata plus a bounded native image preview.
- Report tool failures through Pi's actual failed-tool mechanism rather than
  merely printing error-shaped success text.
- Ensure file writes cooperate safely with Pi's parallel tool execution model.

* DONE Author the =image-asset-design= skill
CLOSED: [2026-09-14 Mon]

- Distinguish generative raster work from deterministic SVG/CSS/canvas/diagram
  work.
- Teach project-style inspection and target-constraint discovery before image
  generation.
- Provide a compact prompt schema covering use, subject, composition, palette,
  style, framing, and exclusions.
- Require visual inspection before accepting output or claiming exact properties.
- Prefer a few deliberate concepts and one targeted edit per iteration.
- Require deterministic cropping, scaling, alpha handling, conversion, and
  target-specific verification before repository delivery.
- Document provenance expectations without turning =SKILL.md= into an API manual.

* STARTED Build deterministic tests

- Unit-test configuration and provider-auth resolution without exposing secrets.
- Test supported and unsupported capability combinations.
- Test confirmation, refusal, cancellation, and total-deadline behavior.
- Test bounded success, provider error, malformed JSON, invalid base64, wrong
  signature, MIME mismatch, oversized response, oversized decoded image, zero
  images, and multiple images.
- Test safe basenames, path traversal, symlink escape, collision refusal,
  partial persistence failure, and no-overwrite behavior.
- Test metadata redaction and compact tool-result details.
- Test native preview size and session serialization so one image cannot bloat a
  session without bound.
- Run type checking, formatting, linting, focused tests, full tests, and a real Pi
  package load smoke test.

* STARTED Complete the billable acceptance test

- [x] Obtain human approval immediately before the first live generation.
- [x] Generate one low-cost square concept into the private draft area.
- [x] Verify reported duration, usage/cost, media type, dimensions, and SHA-256
  against the saved metadata: 11.3 seconds, $0.0676355, and a 1024x1024 PNG.
- [x] Confirm inline TUI rendering and inspect the actual preview with the active
  model; the human also confirmed the result was visible, fast, interesting, and
  fun.
- [ ] Resume the Pi session and confirm the result remains readable without
  excessive session growth.
- [ ] Repeat the same filename once in a future acceptance fixture and prove the
  first artifact is not overwritten.
- [ ] Inspect persisted session data and the worktree for credentials, raw base64
  beyond the required image result, temporary debris, or unrelated changes.

* STARTED Activate and document the accepted capability

- Tool registration was enabled after deterministic tests passed without
  modifying Pi settings or reloading the active session.
- Reload Pi and verify extension-tool and AIPIHKAL skill discovery from an
  ordinary project.
- Keep concise installation, configuration, privacy, cost, and removal behavior
  current in =pi-gregoryg/README.org=.
- Keep implementation rationale in =PLANNING.org= and execution state here until
  the feature reaches a stable stopping point.

* DEFERRED Add image editing and reference inputs

- Support regular local reference files only after text-to-image behavior is
  stable.
- Bound count, aggregate bytes, media types, and dimensions.
- Keep references inside the trusted project or generated-artifact roots.
- Continue to exclude remote URLs until there is a justified use case and a
  tested SSRF-safe fetcher.

* DEFERRED Add one-shot generation preauthorization

- Prefer an explicit =/image-approve-next= command to a permanent spending
  switch.
- Keep grants memory-only, short-lived, fixed-model, limited to one request at no
  more than 1K, and atomically consumed before provider dispatch.
- Do not accept a model-controlled tool argument as evidence of human approval.

* DEFERRED Add model profiles and additional backends

- Evaluate =concept=, =transparent-raster=, and =vector= profiles independently.
- Require explicit endpoint support plus mechanical alpha validation for a
  transparent-raster profile.
- Sanitize generated SVG before preview or promotion; do not autotrace raster
  concepts into vectors.
- Add no provider or model merely to increase the length of a dropdown.
- Consider local GPU or ComfyUI support only when it solves a demonstrated cost,
  privacy, latency, or offline requirement.
- Preserve explicit price and semantic boundaries; never silently fall back to a
  different provider.
